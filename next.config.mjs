/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['store.storeimages.cdn-apple.com', 'www.apple.com', 'is1-ssl.mzstatic.com', 'news-assets.apple.com'],
      },
};

export default nextConfig;
